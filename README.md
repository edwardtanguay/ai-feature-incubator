# ai-feature-incubator

This site has two purposes: (1) a testing ground to develop features with AI for other projects, (2) when a feature is far enough developed, I port it to the project, and leave it here as an example of what can be done with AI.

# setup

- `npm i`
- `npm run dev`
